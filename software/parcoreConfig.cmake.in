@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if (NOT @PARCORE_WITHOUT_ARROW@)
    find_dependency(Arrow)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/parcoreTargets.cmake")

check_required_components(parcore)
